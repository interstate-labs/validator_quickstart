# Prepopulated with the setup for eth-docker: https://github.com/eth-educators/eth-docker. You will have to change this if you setup a different way.

# Beacon client API
BEACON_API_URL="http://localhost:9000"  # eth-docker-consensus-1 (Port 9000)

# Execution client API
EXECUTION_API_URL="http://localhost:8545"  # eth-docker-execution-1 (Port 8545, assuming it's the JSON-RPC API)

# Engine API
ENGINE_API_URL="http://localhost:8545"  # eth-docker-execution-1 (Port 8545, assuming it's also the engine API)

# Engine API JWT
JWT_HEX=""  # To generate the JWT token run: openssl rand -hex 32 | tr -d "\n" > jwtsecret

# Fee recipient
FEE_RECIPIENT=""  # Provide your Ethereum address for receiving fees

# The Bolt RPC port. This port must be exposed!
BOLT_RPC_PORT="18550"  # eth-docker-mev-boost-1 (Port 18550)

# BLS commitment signing key
SIGNING_KEY="your_signing_key_here"  # Provide the appropriate BLS key

# The validator indexes for which to accept commitments. Can be specified as a range i.e. "1..96" (includes 96)
VALIDATOR_INDEXES="your_validator_indexes_here"  # Provide your validator indexes

# Bolt-enabled relay on Helder
BOLT_RELAY="http://0xa55c1285d84ba83a5ad26420cd5ad3091e49c55a813eee651cd467db38a8c8e63192f47955e9376f6b42f6d190571cb5@135.181.191.125:9062"

# Genesis fork version for Helder
GENESIS_FORK_VERSION="0x10000000"
